/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import RefBuilder from './main/RefBuilder';

AppRegistry.registerComponent(appName, () => RefBuilder);
